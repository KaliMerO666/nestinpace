{
  "name": "creative-inc",
  "version": "1.0.0",
  "description": "Nest in Pace - Hugo site with DatoCMS",
  "license": "MIT",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build:datocms": "datocms dump",
    "build:hugo": "hugo --config config.prod.toml --gc --minify",
    "build": "npm run build:datocms && npm run build:hugo"
  },
  "devDependencies": {
    "@datocms/cli": "^1.0.0"
  }
}
```

**Key changes:**
- Changed `dato dump` to `datocms dump`
- Using `@datocms/cli` package

### 2. Update `.nvmrc`
```
20
